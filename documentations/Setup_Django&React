# Setting Up Django Project "Admin3" with a "students" App and React App "admin3"

## Django Project Setup

### 1. Create Django Project and App
- Create a new Django project named "Admin3".
- Create a new app within the project named "students".

### 2. Configure Django Settings
- Ensure the `students` app is added to the `INSTALLED_APPS` in `settings.py`.
- Configure the PostgreSQL database settings in `settings.py`.

```python
# settings.py
