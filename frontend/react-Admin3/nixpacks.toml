# Nixpacks configuration for Railway deployment
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
# Disable CI warnings-as-errors for Railway deployment
cmds = ["CI=false npm run build"]

[start]
cmd = "npx serve -s build -l $PORT"
