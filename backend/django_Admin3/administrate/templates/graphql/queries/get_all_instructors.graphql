query getAllInstructors {
	contacts(filters: [{ field: isInstructor, operation: eq, value: "true" }]) {
		pageInfo {
			hasNextPage
			endCursor
			totalRecords
		}
		edges {
			node {
				id
				legacyId
				firstName
				lastName				
			}
		}
	}
}
