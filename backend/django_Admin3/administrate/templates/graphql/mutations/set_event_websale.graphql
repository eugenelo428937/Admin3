mutation setEventWebsale($eventId: ID!, $websale: String!, $websaleCFKey: ID!) {
	event {
		update(eventId: $eventId, 
			input: { 				
				customFieldValues: [{ definitionKey: $websaleCFKey, value: $websale }] 
			}) {
			errors {
				label
				value
				message
			}
		}
	}
}
